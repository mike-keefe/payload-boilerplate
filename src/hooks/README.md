# Hooks

Payload hooks (`beforeChange`, `afterRead`, `beforeDelete`, etc.) extracted from collection/global configs.

One function per file. Document with JSDoc: what the hook does, which event it runs on, and which fields it modifies.
