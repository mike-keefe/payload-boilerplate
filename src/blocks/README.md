# Blocks

Payload block configs for layout-builder style content (hero, call-to-action, content-image, etc.).

Each block lives in its own file and is composed into content collections via a `blocks` field. The matching frontend renderers live in `src/components/blocks/`.
