# Globals

Payload CMS global configs — singleton documents such as site settings, main navigation, or footer content.

One file per global. Each file exports a single `GlobalConfig` as its default export and is registered in `src/payload.config.ts`.
