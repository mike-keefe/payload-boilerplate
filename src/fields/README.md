# Fields

Reusable Payload field definitions. Anything that would otherwise be copy-pasted between collections (slug field, SEO field group, timestamps, etc.) belongs here.

Export factory functions that return a `Field` or array of `Field`s. Use JSDoc on each to explain the intended use.
