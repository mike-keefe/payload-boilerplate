# UI primitives

Atomic, presentational components — buttons, inputs, badges, cards, etc.

These components should be purely visual: no data fetching, no business logic. They receive everything via props.
