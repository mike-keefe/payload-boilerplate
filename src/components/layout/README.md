# Layout components

Wrappers that structure pages — header, footer, navigation, sidebar, page shells.

Composed from UI primitives in `src/components/ui/`. These may read layout data (nav items, site settings) from Payload globals.
