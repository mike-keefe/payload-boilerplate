# Block renderers

Frontend React components that render Payload blocks defined in `src/blocks/`.

Each file here pairs with a block config: the block defines the shape of the data, this component decides how it looks on the page.
